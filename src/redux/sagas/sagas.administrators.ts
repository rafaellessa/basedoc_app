import {
  AdministratorActions,
  AdministratorTypes,
} from '../reducers/reducer.administrators';

import {BasedocRequestGetAllAdministrators} from '../types/types.administrators';

function* getAllAdministrators() {
  try {
  } catch (error) {}
}
